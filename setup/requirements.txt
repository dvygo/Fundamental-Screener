crawl4ai>=0.6.0
beautifulsoup4>=4.12.0
lxml>=5.0.0
httpx>=0.27.0
feedparser>=6.0.0   # news RSS (livemint, moneycontrol, …)

# storage + query (ELT: land raw -> MinIO, query with DuckDB)
minio>=7.2.0        # push raw/bronze to MinIO (S3), WORM object-lock
duckdb>=1.5.0       # shred XBRL -> parquet; schema-on-read screens over MinIO
